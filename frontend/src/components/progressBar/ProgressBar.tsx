import { Box, LinearProgress } from "@mui/material";

const ProgressBar = () => {
   return (
      <Box sx={{ width: "100%" }}>
         <LinearProgress />
      </Box>
   );
};

export default ProgressBar;